services:
  mahjong:
    build: .
    container_name: mahjong-online
    ports:
      - "3000:3000"
    volumes:
      - mahjong-data:/usr/src/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  mahjong-data: